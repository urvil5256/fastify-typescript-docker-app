enum Roles {
  admin = "Admin",
  employee = "Employee",
  user = "User",
}

enum Organization {
  RadixWeb = "radixWeb",
}

enum Deparment {
  Account = "account",
  Admin = "admin",
  HR = "hr",
  IT = "it",
}

export { Roles, Organization, Deparment };
