enum Roles {
  Admin = "admin",
  Empoyee = "employee",
}

enum Organization {
  RadixWeb = "radixWeb",
}

enum Deparment {
  Account = "account",
  Admin = "admin",
  HR = "hr",
  IT = "it",
}

export { Roles, Organization, Deparment };
